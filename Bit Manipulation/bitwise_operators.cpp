#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int a=5,b=14,c=11;
    cout<<(a&b)<<endl; //Bitwise AND  
    cout<<(a|b)<<endl; //Bitwise OR
    cout<<(a^b)<<endl; //Bitwise XOR  
    cout<<(11<<1)<<endl; //Left shift
    cout<<(11<<2)<<endl;
    cout<<(11>>1)<<endl; //Right shift
    cout<<(11>>2)<<endl;
    
    return 0;
}